/** Shared button looks, so every Cancel reads the same soft red everywhere. */
export const cancelButton =
  'rounded-md border border-danger/40 px-4 py-2.5 text-sm font-semibold text-danger-text hover:bg-danger/10'
